spring:
  application:
    name: springboot-boilerplate
  cloud:
    config:
      enabled: false
aws:
  paramstore:
    enabled: true
    prefix: /${ENV_NAME}
    failFast: true
    name: springboot-boilerplate